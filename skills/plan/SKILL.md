---
name: plan
description: "[Legacy alias] Use /think instead. Produce an implementation plan."
allowed-tools: Read, Glob, Grep, Bash
---

# /plan — legacy alias

**Use `/think` instead.** See `skills/think/SKILL.md`.
