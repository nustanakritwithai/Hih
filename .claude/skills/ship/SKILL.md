---
name: setup
description: "[Legacy alias] Use /awaken instead. Bootstrap Dioo in a repository."
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# /setup — legacy alias

This skill is deprecated. Dioo now uses life phases instead of work verbs.

**Use `/awaken` instead.** See `skills/awaken/SKILL.md` for the full contract.
