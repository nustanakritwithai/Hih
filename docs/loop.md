# The default Hih loop

> **Deprecated.** See `workflows/default/loop.md` for the Dioo life cycle.

Legacy five-verb loop: setup → plan → work → review → ship.

Each verb maps to a Dioo life phase. Use `/awaken`, `/think`, `/act`, `/reflect`,
`/grow` instead.
